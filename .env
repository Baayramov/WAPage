# WAPage — Environment Variables

# Program data folder (automatically assigned if left blank)
# Default: %APPDATA%/WAPage
APP_DATA_DIR=

# Log level: trace, debug, info, warn, error, critical
LOG_LEVEL=info

# Log files maximum file size (MB)
LOG_MAX_SIZE_MB=10

# Log files maximum count (rotation)
LOG_MAX_FILES=3
